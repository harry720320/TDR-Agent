# TDR Agent Configuration
# Copy this file to .env and update the values

# Target API hostname (with port if needed)
# Uses HTTPS by default. For HTTP, specify full URL: http://api.example.com:8080
TDR_HOSTNAME=api.example.com:8080

# API token for authentication (will be sent in X-API-KEY header)
TDR_API_TOKEN=your_api_token_here

# OpenAI API configuration (optional - for complex queries)
OPENAI_API_KEY=sk-your_openai_api_key_here
OPENAI_MODEL=gpt-5-mini
